# Tend's serverless functions (api/parse.py, api/feed.py) use only the Python
# standard library — no packages needed. This file's presence signals to Vercel
# that the project includes Python so it provisions the Python runtime.
