# Tend deploys on Vercel as a static site PLUS one Python serverless function
# (api/index.py — the Phase B bank / Trading 212 API). Do NOT ignore api/.
#
# The function only does anything once its Environment Variables are set in the
# Vercel project AND config.js → BACKEND_URL is "/api". Until then it's dormant.

# Local dev / archive cruft
_archive_nextjs/
__pycache__/
*.pyc
.env
